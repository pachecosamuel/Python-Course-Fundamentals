hard_carry = {'anti-mage', 'medusa', 'void', 'spectre'}
agility_heroes = {'anti-mage', 'muerta', 'ursa', 'spectre'}
hard_carry.add('chaos-knight');
a, b = divmod(10, 3);


def megabite():
    return pow(1024, 2);


def circle_area(radius):
    return radius * 3.141592;


def cylinder_area(radius, height):
    return circle_area(radius) * height


print(cylinder_area(3, 5))